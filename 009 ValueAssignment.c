#include <stdio.h>

void main()

{
    int x;
    x = 10 ;
    int y = 20 ;
    printf(" The two variables are %d and %d",x,y);
    // you can either assign type seperately and then assign value or do both at the same time
}